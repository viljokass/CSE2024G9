Feature: showing off behave

  Scenario: run a simple test
     Given we have behave installed
      When we implement a test
      Then behave will test it for us!

  @wip
  Scenario: run a simple test again
     Given we have behave installed
      When we implement a test
      Then behave will test it for us!